import { Box, Grid } from "@mui/material";

const BlogComponent = () => {
    return (
       <Box>
        <Grid container spacing={2}>
            
        </Grid>
       </Box>
    );
}

export default BlogComponent;